// -  Create a variable named `baseNum` and assign the value `123` to it
// -  Create a function called `doubling` that doubles it's input parameter and returns with an integer
// -  Log the result of `doubling(baseNum)` to the console

'use strict';
export {};
let baseNum: number = 153;
let double: number = 12;
function doubling (a: number, b: number) {
  return a * b;
}
doubling(baseNum, double);
console.log(doubling(baseNum, double));
